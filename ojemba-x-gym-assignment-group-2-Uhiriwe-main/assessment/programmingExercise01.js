/**
 * PROGRAMMING EXERCISE 01:
 *
 * Write an arrow function that takes two values x and y as arguments.
 * Return true if the two values are equal and of the same type.
 */
function num(x, y)
{
    var z;
    this.getCLazy = function() {
        if (z === undefined) z = x * y;
        return z;
    }
 }